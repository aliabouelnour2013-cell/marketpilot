# Contributing to MarketPilot

1. Create a branch for your change.
2. Keep secrets out of Git.
3. Run `npm install`.
4. Run `npm run build`.
5. Test the affected functionality.
6. Open a pull request with a concise description.

MarketPilot should clearly distinguish demo data, provider data, calculated data, and AI interpretation.
